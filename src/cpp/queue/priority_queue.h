#pragma once

#include "request_queue.h"

namespace atomic {
/**
 * Namespace for priority-queue related types and utilities.
 *
 * Serves as a modular placeholder that exposes or extends priority queue
 * functionality; the concrete implementation is provided in request_queue.h.
 * Keep this namespace for organization and future extensions.
 */
namespace queue {

// Priority queue is implemented in request_queue.h
// This header exists for modularity and future extensions

} // namespace queue
} // namespace atomic